import NivelScroll from './nivel-scroll'

// La portada queda dedicada a la bienvenida y a la progresión de niveles.
const Landing = (): JSX.Element => (
  <div className="home-page">
    <NivelScroll />
  </div>
)

export default Landing
